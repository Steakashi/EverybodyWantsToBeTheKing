const TIMEOUT = 30000;
const TURNTIME = 60;

const STATUS = {
    UNKNOWN : "unknown",
    CONNECTED : "connected",
    DISCONNECTED : "disconnected",
    PLAYING : "waiting",
    READY : "ready",
}

module.exports = { TIMEOUT, TURNTIME, STATUS }